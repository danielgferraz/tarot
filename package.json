{
  "name": "mystic-tarot-canvas",
  "version": "1.0.3",
  "description": "Backend Serverless e Frontend Static para MysticTarot",
  "main": "api/index.ts",
  "scripts": {
    "build": "mkdir -p public && cp index.html index.tsx App.tsx types.ts constants.ts metadata.json public/ && cp -r components services lib public/"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.1",
    "@vercel/node": "^3.2.16"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}